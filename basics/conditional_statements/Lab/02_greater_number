function greaterNumber(numbers) {
    let [num1, num2] = numbers.map(Number)
    if (num1 > num2) {
        console.log(num1);
    }
    else {
        console.log(num2)
    }
}

// greaterNumber((["5", "3"]))