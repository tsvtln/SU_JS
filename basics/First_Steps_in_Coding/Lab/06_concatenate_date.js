function ConcatenateData(input) {
    let [name, surname, age, city] = input;
    console.log(`You are ${name} ${surname}, a ${age}-years old person from ${city}.`);
}

ConcatenateData(["TSVTLN", "TSV", "30", "SOFIA"])
